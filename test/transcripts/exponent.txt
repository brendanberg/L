>> Integer n ('^': m) -> { pow :: {{
 -     (_, 0) -> {1},
 -     (x, 1) -> {x},
 -     (x, y) ? (y > 0) -> { pow(x * n, y - 1) }
 -   }}, pow(n, m)
 - }
Integer n ('^': m) -> {
    pow :: {{
        (_, 0) -> { 1 }
        (x, 1) -> { x }
        (x, y) ? (y > 0) -> { pow(x * n, y - 1) }
    }}
    pow(n, m)
}
>> 2^0
1
>> 2^1
2
>> 2^10
1024
>> 2^16
65536
>> 2^32
4294967296
